load("@com_github_bazelbuild_buildtools//buildifier:def.bzl", "buildifier_test", "buildifier")

buildifier(
    name = "buildifier",
)

buildifier_test(
    name = "buildifier_test",
    srcs = glob([
        "**/*.bazel",
        "**/*.bzl",
    ]),
)
