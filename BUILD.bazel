load("@com_github_bazelbuild_buildtools//buildifier:def.bzl", "buildifier", "buildifier_test")
load("//:package.bzl", "package_binary")

buildifier(
    name = "buildifier",
)

buildifier_test(
    name = "buildifier_test",
    srcs = glob([
        "**/*.bazel",
        "**/*.bzl",
    ]),
)

package_binary(
    name = "skyscope",
    binary = "//backend:skyscope",
    description = "Skyscope",
    entrypoint = "//:entrypoint.sh",
)
