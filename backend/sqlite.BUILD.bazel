load("@rules_cc//cc:defs.bzl", "cc_library")

cc_library(
    name = "sqlite.dev",
    #srcs = glob(["lib/*.so*"]),
    srcs = [],
    hdrs = glob(["include/*.h"]),
    strip_include_prefix = "include",
    visibility = ["//visibility:public"],
)
